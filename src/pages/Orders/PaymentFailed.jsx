import { Typography } from '@material-ui/core'
import React from 'react'

const PaymentFailed = () => {
    return (

        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', flex: 1 }}>
            <Typography>Payment Fail</Typography>
        </div>

    )
}

export default PaymentFailed
